import { AdmissionPublicForm } from '@/components/admissions/admission-public-form';

export const metadata = { title: 'Sistem Penerimaan Murid Baru' };

export default function Page() {
  return <AdmissionPublicForm />;
}
